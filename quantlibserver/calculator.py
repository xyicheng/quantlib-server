class Calculator(object):
    def price(self, underlying_price, strike, interest_rate, volatility, day_to_expiry, dividend_yield ):
        #return underlying_price + strike + interest_rate + volatility + day_to_expiry + dividend_yield;
        return 38.70
