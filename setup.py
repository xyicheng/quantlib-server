from setuptools import setup

setup(name='quantlibserver',
      version='0.1',
      description='Exposing quantlib via zerorpc interface',
      url='http://github.com/yksi7417/quantlib-server',
      author='Anthony Si',
      author_email='yatkasi@gmail.com',
      license='Apache 2.0',
      packages=['quantlibserver'],
      zip_safe=False)

